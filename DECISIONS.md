# DECISIONS.md — lann-site 关键决策记录

记录涉及架构、否决方案、踩坑的重大决策。常规开发不需要记录，只记录"为什么这样做"将来会被问到的内容。

格式：

```
## [日期] 决策标题
- 背景：
- 决策：
- 为什么：
- 备选方案（如有）：
```

---

## 2026-09-02 Dashboard 与 Site 回迁 LANN 阿里云，Data 通过只读版本包交付

- 背景：Dashboard 与 Site 部署在 Hanson 服务器后，应用发布、定时任务、目录权限和生产核验均需要跨人协作，影响双方效率；此前 VPN 与 SSH 入口也多次干扰正常运维。
- 决策：Dashboard 与 Site 由 LANN 自有阿里云运行；Data 继续由 Hanson 侧治理，只通过 HTTPS 发布不可变、版本化、脱敏的月度聚合包。Site 在阿里云本地校验、缓存和分析，Dashboard 只读 Site 输出。跨服务器边界不使用数据库直连、BI 凭证、VPN、SSH 或 Hanson 代执行 Site 代码。
- 为什么：应用运行权应归业务系统所有方，数据发布权仍归正式数据治理方。版本包能把双方依赖收敛为可验证契约，并支持失败时沿用最近成功数据和独立回滚。
- 备选方案：继续全部运行在 Hanson 服务器——放弃，因为运维职责和协作成本不匹配；仅迁 Dashboard——放弃，因为 Site 发布仍需跨人协作；迁移 Data——暂不采用，因为会制造第二套数据治理源。

## 2026-07-19 好店三层 Gate、零碎月与反向结果例外

- 背景：经营数据已接入 Hanson 日结、新老客、留存和返店频次，但新店开业零碎月会扭曲月均营收与波动；部分门店不足12个月即已终止经营。
- 决策：完整好店采用经济性、稳定性、客户健康三层 Gate。经济性首版为平均月营收不低于28万元、租售比不高于15%，其中15%是待持续验证的首版基准，不是永久标准；稳定性首版为至少12个完整经营月、营收CV不高于10%、近3月较此前不低于-10%；客户健康为留存率或返店频次至少一项不低于同批样本中位数。开业零碎月不进入月度 Gate，数据不足时转入日级爬坡分析。已终止且至少有6个有效经营月的门店，可作为不足12个月的反向结果例外。战略门店的战略价值与经济性结果分开表达。SABC 当前是综合商场等级、门店业绩和公司规划形成的业务讨论标签，用于选择同类对照，不作为模型真值或固定公式。
- 为什么：避免用不完整月份制造伪精确结论；同时不能因为门店提前终止而无法进入反向样本库。三层 Gate 可以把位置带来的经济性、经营稳定和客户沉淀分开验证。
- 备选方案：所有门店统一等待12个月再定性——放弃，因为会丢失已终止门店的明确结果；按固定自然月直接计算——放弃，因为开业零碎月会系统性低估新店。

## 2026-07-10 新建 lann-work-bot 承载事项编排机器人

- 背景：本机旧 `Lann-bot-backup-20260525` 具备飞书 webhook、消息处理、图片识别、文档读取和 Railway 部署经验，但项目目录和 `index.js` 噪音较大，混有旧记忆、日历、诊断、测试和 dashboard 历史逻辑，继续在其上开发会增加事项编排入口的复杂度和维护风险。
- 决策：新建独立项目 `lann-work-bot` 承载飞书机器人事项编排。旧 bot 仅作为参考和局部代码来源，不直接继续堆代码。`lann-site` 的 MD 文件和 AI 任务定义作为项目状态源接入新 bot。
- 为什么：事项编排需要稳定、低噪音、边界清晰的入口项目；新项目可以只围绕 `feishu_bot_inbox_ingest`、`source_packet`、`project_status_read`、`work_item_orchestration` 设计。
- 备选方案：直接改旧 Lann-bot 备份项目——放弃，因为旧项目历史包袱过重；把机器人逻辑放进 `lann-site`——放弃，因为 `lann-site` 是选址分析判断引擎，不应承载在线机器人服务。

## 2026-07-10 飞书机器人作为事项编排统一入口

- 背景：飞书文档收件箱、本地 inbox、CMD 粘贴都不符合王凯真实工作流。微信在手机端、飞书在电脑和手机端、想法多来自即时语音，若要求王凯找文档、插图、补文字或导出文件，会显著增加工作成本。飞书机器人已有现成功能可连接，适合作为消息流入口和输出位置。
- 决策：事项编排项目入口改为飞书机器人。王凯通过机器人发送会议妙记链接、云文档链接、微信截图、语音说明、电话/想法补录和飞书转发消息；机器人每天晚上默认 21:30 提醒同步资料；王凯发送“开始编排”后，系统读取当天机器人收件资料、本地 `lann-site`、`lann-dashboard` 和工作OS状态，输出事项编排到同一机器人会话。
- 为什么：聊天流是王凯最低成本的信息输入方式，也便于承载截图、语音、链接和转发消息。机器人入口可以保留原始资料引用，再生成派生事项，降低 AI 误解后无法追溯的风险。
- 备选方案：飞书云文档收件箱——放弃，因为手机端编辑成本高且大量图片/语音/链接会混乱；本地 inbox 文件夹——放弃，因为微信和想法不在本地；CMD 粘贴——放弃，因为会把整理成本转嫁给王凯。

## 2026-07-10 第一阶段最高优先级调整为事项编排

- 背景：王凯在处理日常待办时发现，当前工作台上的 `lann-site`、`lann-dashboard`、工作OS 三个项目和日常飞书待办仍是散乱的。飞书 AI 助手可以列待办，但难以识别事项之间的依赖、冲突、重复和组合机会；多个待办放在一起看，可能产生新判断，甚至推翻原方案。
- 决策：在 AI 矩阵第一阶段中新增并优先推进 `work_item_orchestration`（事项编排）任务。短期不做复杂系统，不自动改飞书待办、不自动派任务、不自动发消息；先用轻量模板和结构化 schema，让 AI 每天帮助识别“哪些只是执行，哪些其实在改变判断”。
- 为什么：王凯当前效率瓶颈不是缺待办清单，而是缺“事项关系层”。先跑通事项编排，可以直接提升每天处理待办、项目推进和判断更新的效率，也能为后续 dashboard 状态展示和工作OS判断沉淀提供更清晰的输入。
- 备选方案：继续优先做选址初筛 eval 或外部选址情报样本——暂缓，因为它们提升的是单个业务链路效率；事项编排提升的是所有项目和日常工作的协同效率。

## 2026-07-09 连锁经营部 AI 矩阵先行，三项目同步做前置工作

- 背景：王凯确认当前工作主要围绕飞书沟通、飞书妙记、选址分析、经营归因、dashboard 展示和工作OS判断沉淀展开。未来需要支持 AI 矩阵、多模型插拔，以及外部选址数据接入。若直接分别改造 `lann-site`、`lann-dashboard`、工作OS，容易形成三套命名、三套数据口径、三套 prompt 和三套权限规则。
- 决策：先定义“AI矩阵与三项目分工 v0.1”，再基于它同步推进 `lann-site`、`lann-dashboard`、工作OS 的前置工作，并把第一阶段目标定为：让 AI 稳定回答“这件事是什么、关联哪个业务对象、资料是否足够、下一步该补什么、是否值得王凯看”。外部新闻、点评、小红书、地图、热点等数据单独归口为 `lann-site` 的“外部选址情报层”，只作为选址分析 AI 的输入，不直接进入 dashboard 或工作OS。
- 为什么：模型会变化，但业务对象、判断流程、权限边界、输出 schema 和评测样本必须稳定。外部数据对当前业务的主要价值在选址，因此应先由 `lann-site` 做结构化、可信度标注和证据归档，再输出给 dashboard 展示或工作OS沉淀方法论。
- 备选方案：先分别改造三个项目——放弃，因为会增加后续多模型插拔和跨项目协作成本；先做统一大平台——放弃，因为当前更缺稳定数据口径、任务定义和评测样本。

## 2026-07-03 测算表抽取从默认总额改为驱动假设，真实表现以 BI 为准

- 背景：按“2023 年后开业 + 有测算”圈出 46 家试点后，验证发现测算表仍有约 22 种标签结构，虽可聚成少数模板家族，但抽出的月营收值高度集中，疑似大量来自模板默认假设，而非逐店精算。继续硬抽默认营收总额会把低可信分母带入后续租售比和达成率判断。
- 决策：暂停把测算表默认月营收作为核心判断依据。测算表后续只抽“业绩预估 + 支撑驱动”，例如新客数、老客数、客单价、客均时长；真实经营表现、租售比和样本分层优先使用 BI 实际经营数据与飞书结构化租金数据。
- 为什么：驱动假设比模板总额更能反映立项时的业务判断，也更容易解释新客获取动能、客单与服务能力等选址假设。BI 实际经营数据更适合作为现有门店正反样本和新点位对标基准。
- 备选方案：继续扩展测算表解析器并抽取默认营收总额——放弃，因为会增加大量模板适配成本，且核心字段可信度不足。

## 2026-06-21 lann-site 与 lann-dashboard 拆分为独立仓库，CLAUDE.md 走精简路线

- 背景：lann-site 目录初始的 CLAUDE.md 实际是 lann-dashboard 项目的内容（描述了另一个项目的目录结构、技术栈、权限规则），与本仓库的实际定位（选址分析 + AI 判断，独立仓库）不符。git remote 当时也未配置。
- 决策：lann-site 重新编写专属 CLAUDE.md，只保留跨项目通用的协作纪律（推进节奏、权限边界、git 纪律、文档同步），去掉 dashboard 专属的技术栈约定、架构演进叙述、具体飞书表清单。技术栈定为 Python。
- 为什么：避免 CLAUDE.md 随代码膨胀；两个仓库各自独立维护身份核验信息，避免历史教训（git remote 配置错误导致部署拉不到新代码）重演。
- 备选方案：直接复用 dashboard 的 CLAUDE.md 增量修改——放弃，因为内容耦合过深，长期会越改越乱。
