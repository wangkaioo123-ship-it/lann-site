# CLAUDE.md — lann-site 项目指引

本文件是 lann-site 项目的协作规则与身份依据。每次新会话开始时必须先读取本文件、DECISIONS.md 全文、DEVELOPMENT_LOG.md 最近 7 天记录，并执行身份核验（见五）。

---

## 一、项目身份

- 项目：lann-site — LANN 连锁选址分析与 AI 判断
- 定位：独立仓库，负责选址因子分析与"好选址"判定；通过飞书多维表格作为数据契约层，与 lann-dashboard（展示层）对接。不共享代码、依赖、配置。
- 技术栈：Python（数据分析 + AI 判断），具体框架/依赖随开发推进确定，不提前锁定
- 仓库地址：https://github.com/wangkaioo123-ship-it/lann-site.git
- 数据设计文档：维护在 `docs/` 目录（当前 v0.1）。字段 schema、口径定义、抽取优先级等以该文档为准，不在本文件重复。

---

## 二、关于我

- 王凯（Kai），LANN 连锁经营部负责人，纯业务出身，不熟悉代码细节，但有基本技术常识
- 工作语言：默认中文，代码本身用英文，技术术语可中英混用
- 沟通偏好：objective、给选项和权衡，不需要赞美和情绪铺垫；代码与文档风格追求清晰可读，不堆术语

---

## 三、推进节奏

不要一次写完所有代码，按步骤推进：

1. 先输出方案（数据/技术设计、文件清单）
2. 我 review 后，开始实现
3. 本地验证通过后，再考虑是否需要部署/对接

每一步完成后告诉我，我确认后再进入下一步。

涉及业务逻辑、选址判断标准的问题，多问我，不要自己猜测；技术实现的选择可以自主决定，简要说明理由即可。

---

## 四、权限边界

**可以自主执行（不需要确认）：**
- 在 lann-site 目录内新建、修改、重构代码
- 安装本项目需要的依赖
- 本地运行测试、调试命令

**必须先确认：**
- 删除已有文件、删除超过 50 行代码、覆盖已有配置文件
- 向飞书多维表格写入或修改数据/字段/权限（即使是测试数据）
- `git push`、`git push --force`、`git reset --hard`、`git clean -fd`、修改 git remote、删除分支
- 跨项目操作（涉及 lann-bot / lann-dashboard 任何文件）
- 申请新的 API 密钥、改动会产生费用的服务配置
- 把密钥、token 写入代码或日志；涉及敏感信息时提交代码到 Git

不确定算不算需要确认的范畴时，默认按需要确认处理。

---

## 五、Claude Code 协作纪律

源于其他项目的真实事故（git remote 配置错误导致部署灾难），精简后作为 lann-site 的基本纪律：

- **身份核验**：每次新会话第一动作执行 `git remote -v`，核对地址是否为 `https://github.com/wangkaioo123-ship-it/lann-site.git`。不一致立即停止并报告，不假设配置正确。
- **不单方面声称"已完成"**：每次改动后说明：改了哪些文件、是否已 commit（commit ID）、是否已 push、用户需要做什么才算验证通过。禁止用"应该好了""修复成功"代替实际验证。
- **症状与根因可能不在同一层**：同一方向尝试 3 次未解决，停止并向用户说明判断和需要的信息，不继续盲猜。
- **降级方案/高风险操作先报警**：涉及认证方式降级、暴力清理进程（taskkill/kill）、强制 git 操作、修改 CI/CD 或基础设施配置时，先用【需要确认】格式说明动作、原因、风险、替代方案，等批准。
- **发现配置混乱时先报告不动手**：git remote 不符合预期、目录混入其他项目文件、依赖或密钥多处不一致等情况，停下来报告事实和影响，不擅自"顺手修复"。
- **不终止本地服务进程**：本地测试用 background 方式启动，不用 taskkill/kill 管理进程生命周期。

---

## 六、文档结构

| 文档 | 定位 | 更新时机 |
|---|---|---|
| CLAUDE.md（本文件） | 协作规则与项目身份，保持精简，不随代码膨胀 | 协作规则变化时，Claude Code 提醒，用户决定是否更新 |
| DECISIONS.md | 关键决策、否决方案、踩过的坑 | 重大决策发生时，Claude Code 提醒，用户决定是否记录 |
| DEVELOPMENT_LOG.md | 按时间记录每次开发/修复的内容 | 每次完成功能或修复后，Claude Code 必须追加记录 |
| docs/ | 数据设计、字段 schema、领域方法论等随项目演进的内容 | 随设计迭代更新，版本号递增 |

新会话开场顺序：CLAUDE.md → DECISIONS.md → DEVELOPMENT_LOG.md（近 7 天）→ 身份核验 → 向用户简述理解 → 等待确认后开始工作。
